{
  "name": "railway-live-betting",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm run build",
    "start": "npm run build && node -e \"console.log('✅ Frontend build tamamlandı, service hazır')\" "  // Sadece build, frontend static
  }
}
